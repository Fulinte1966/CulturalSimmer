# Brand asset

`wordmark.svg` is the supplied local `文火` logo. It is a path-only SVG with no
font, raster-image, script, or external-resource dependency.

Use it unchanged. Do not redraw, trace, recolor, simplify its paths, or replace
it with an unrelated asset. The containing link must provide an accessible
`文火` label.

SHA-256:

```text
B22E8ED84B2551C0B6049A63AB9D2DEBF991DFB42A4146819171683B93BE6998
```

See `reference/figma-handoff/assets-status.json` for provenance.
