---
export interface OutlineItem {
  level: number;
  title: string;
  page: number;
}

interface Props {
  outline: OutlineItem[];
}

const { outline } = Astro.props;
---

{
  outline.length > 0 && (
    <section class="content-section outline" aria-labelledby="outline-heading">
      <h2 id="outline-heading">目录大纲</h2>
      <ul class="outline-list">
        {outline.map((item) => (
          <li
            class="outline-item"
            style={`padding-left: ${(item.level - 1) * 1.25}rem`}
          >
            <span class="title">{item.title}</span>
            <span class="page">{item.page}</span>
          </li>
        ))}
      </ul>
    </section>
  )
}
